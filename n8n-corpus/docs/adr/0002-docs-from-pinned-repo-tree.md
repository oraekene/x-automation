# Docs come from the pinned repo tree, not live docs.n8n.io

The live docs site is generated from the n8n-docs repo's `main` branch and redeploys on every commit, so it reflects a newer state than any pinned SHA in the Corpus. Taking Docs content from the pinned repo tree keeps docs and workflows version-consistent within a snapshot. The live site is never crawled.

Status: accepted
