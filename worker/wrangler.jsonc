{
  "name": "x-automation-worker",
  "compatibility_date": "2025-07-18",
  "main": "src/index.ts",
  "d1_databases": [
    {
      "binding": "DB",
      "database_name": "x-automation",
      "database_id": "local-dev-placeholder",
      "migrations_dir": "migrations"
    }
  ],
  "//": "Provision the real database before first deploy: `npm run db:create`, then `npm run db:migrate`, then paste the returned database_id above.",
  "//crons": "Cron triggers (tick/maintenance/budget-reset) arrive in ticket 04 — do not declare them here until the scheduled handler exists."
}